
config:
  target: "http://localhost:8181"
  phases:
    - duration: 120
      arrivalRate: 20

scenarios:
  - name: "Get timestamp"
    flow:
      # Get ten timestamps
      - get:
          url: "/timestamp"
      - get:
          url: "/timestamp"
      - get:
          url: "/timestamp"
      - get:
          url: "/timestamp"
      - get:
          url: "/timestamp"
      - get:
          url: "/timestamp"
      - get:
          url: "/timestamp"
      - get:
          url: "/timestamp"
      - get:
          url: "/timestamp"
      - get:
          url: "/timestamp"
